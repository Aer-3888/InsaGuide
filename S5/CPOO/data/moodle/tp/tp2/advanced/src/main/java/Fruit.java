/**
 * Abstract base class representing a fruit produced by a tree.
 * Different tree species produce different types of fruits.
 *
 * <p>This class is used with generics to ensure type safety:
 * oak trees produce acorns, pine trees produce pine cones.
 *
 * @author INSA Rennes - CPOO TP2
 * @version 1.0
 */
public abstract class Fruit {
    // Empty abstract class serving as a type hierarchy root
    // Concrete subclasses: Gland (acorn), Cone (pine cone)
}
